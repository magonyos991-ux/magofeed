{
  "name": "magofeed",
  "private": true,
  "version": "1.0.0",
  "description": "Magofeed — la carte communautaire des boissons introuvables (app native Capacitor)",
  "scripts": {
    "build:web": "node scripts/assembler-www.mjs",
    "sync": "npm run build:web && npx cap sync",
    "android": "npm run build:web && npx cap sync android && npx cap open android",
    "ios": "npm run build:web && npx cap sync ios && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/core": "^6.2.1",
    "@capacitor/ios": "^6.2.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.1"
  }
}
