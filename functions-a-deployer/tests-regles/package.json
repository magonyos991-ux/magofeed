{
  "name": "magofeed-tests-regles",
  "private": true,
  "type": "module",
  "scripts": {
    "pretest": "node -e \"require('fs').copyFileSync('../firestore.rules','firestore.rules')\"",
    "test": "firebase emulators:exec --only firestore --project magofeed-test \"node regles.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "10.12.2",
    "firebase-tools": "^13.35.1"
  }
}
