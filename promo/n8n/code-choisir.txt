// La carte visuelle correspond TOUJOURS a la boisson choisie (banque de 12
// cartes, 4 designs). Plus de dependance a OpenFoodFacts pour l'image.
const prev = $('Controle qualite - fusion').first().json;
const CARDS = ["guarana-antarctica","ramune","calpico","mogu-mogu","inca-kola","milkis","ciao-energy","jarritos","sumol","pocari-sweat","vimto","chupa-chups"];
const slug = String(prev.imageQuery || "").toLowerCase().trim();
const base = "https://magonyos991-ux.github.io/magofeed/promo/cards/";
const imageUrl = base + "card-" + (CARDS.indexOf(slug) !== -1 ? slug : "generic") + ".jpg";
return [{ json: { ...prev, imageUrl: imageUrl } }];
